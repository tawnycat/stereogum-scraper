<div class="row">
	<div class="col-md-12 border rounded m-3 p-3">
		<h5><a href="{{link}}">{{title}}</a></h5>
		<p>{{preview}}</p>
		<br>
		<button type="button" class="btn btn-secondary leaveComment" data-id="{{_id}}">Leave comment</button>
		<button type="button" class="btn btn-secondary viewComments" data-id="{{_id}}">View comment</button>
	</div>
</div>

